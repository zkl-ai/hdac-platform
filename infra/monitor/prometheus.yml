global:
  scrape_interval: 15s
  evaluation_interval: 30s

scrape_configs:
  - job_name: 'jetson'
    file_sd_configs:
      - files:
          - '/data/workspace/hdap-platform/infra/prometheus/targets.json'
        refresh_interval: 30s

rule_files: []

